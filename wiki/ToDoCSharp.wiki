#summary ToDo в C# версии

  # Построение трубы из каркаса.
    # *(В)* ~~класс ветви трубопровода~~
    # удаление плохих сегментов.
    # *(П)* предрасчетные тесты
    # свертывание-развертывания джойнтов (подумать, можно ли сделать его однократным, чтобы сегменты всегда были "развернуты". Подробности тут: http://code.google.com/p/pipe3/issues/detail?id=86
    # трансформации каркаса - рекурсивный перерасчет дочерних веток
  # Отображение модели трубопровода (доработать сцену OpenGL):
    # *(В)* ~~отображение всех типов сегментов с необходимыми атрибутами~~
    # отображение всех типов сегментов с такими же цветами/прозрачностью, как в Дельфи
    # *(П)* ~~интерфейс поворотов/увеличения - такой же как в дельфи~~
    # *(П)* ~~доделать сдвиг по правой кнопке мыши~~ 
    # *(В)* отображение текстур
    # *(В)* ~~отображение выделенного сегмента~~
  # Объектная модель:
    # *(В)* класс глобальных свойств трубопровода
    # классы баков (не забыть про загрузку калибровки из csv)
    # продумать "временный" поворот трубопровода как целого при нестационарном расчете. Он должен отображаться в главном окне и пересчитывать координаты для расчетной части. При этом возвращаться в исходное состояние после расчета.
    # *(В)* ~~запись / чтение модели трубопровода~~
  # Интерфейс:
    # *(М)* доработать редактор каркаса
    # *(В)* ~~дерево сегментов для отображения/выбора ветвей и сегментов в главном окне~~
    # *(В)* ~~редактор свойств сегментов (может быть, разместить его рядом (под) деревом сегментов)~~
    # редактор баков (с редактором калибровки пользовательского бака!)
    # графики посегментных параметров
    # графики временнЫх параметров
    # геометрические операции с каркасом
    # *(М)* экспорт в Excel
  # Расчетная часть
    # перевод общих функций расчета (CalcLoss)
    # рекурсивный обход дерева трубопровода
    # перевод расчета джойнтов
    # пересчет результатов в текстуры и т.п. (CalcResults) ? как сделать ссылку на массив расчетных параметров ?